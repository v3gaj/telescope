<% if @skill.errors.any? %>
  $('#render-skill-errors').html("<%= j (render :partial =>'skills/errors') %>");
<% else %>
	$('#render-user-skill').html("<%= j (render :template => 'skills/index') %>");
	skill_update_js();
<% end %>